# Task 12 — README

**Depends on:** Task 11 (final state of the app)

## Sections
1. **Setup & run** — backend (`pip install -r requirements.txt && uvicorn main:app --reload`) + frontend (`npm install && npm run dev`)
2. **Assumptions & decisions** — tech choices, trade-offs, scope limits (top 10 tickers, SQLite, no auth)
3. **What I'd do with more time** — AI bonus, WebSocket real-time updates, PostgreSQL, tests

## Done when
- A fresh user can clone and run the project using only the README
