# Task 11 — Styling + polish

**Depends on:** Tasks 07–10

## Steps
- CSS for the main grid layout (responsive proportions)
- Loading spinners while data fetches
- Error states: API unreachable, ticker data unavailable
- Consistent color scheme (dark/light, your choice)
- Final integration test: ticker select → chart/table render → note CRUD end-to-end

## Done when
- App looks presentable and usable
- No unhandled loading/error states visible to user
